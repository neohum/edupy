# Resend 이메일 설정 가이드

오류 보고 기능을 사용하려면 Resend 계정을 설정해야 합니다.

## 1. Resend 계정 생성

1. https://resend.com/ 접속
2. "Start Building" 또는 "Sign Up" 클릭
3. GitHub 계정으로 로그인 (또는 이메일로 가입)
4. 무료 플랜: 월 3,000개 이메일, 일 100개 이메일

## 2. API Key 발급

1. Resend 대시보드 로그인
2. 왼쪽 메뉴에서 "API Keys" 클릭
3. "Create API Key" 클릭
4. 이름 입력 (예: "EduPy Error Reports")
5. Permission: "Full access" 선택
6. "Add" 클릭
7. **API Key 복사** (한 번만 표시됨!)
   - 형식: `re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx`

## 3. 도메인 인증 (선택사항)

### 옵션 A: Resend 기본 도메인 사용 (빠른 테스트)
- 발신자: `onboarding@resend.dev`
- 바로 사용 가능
- 제한: 자신의 이메일로만 발송 가능

### 옵션 B: 자신의 도메인 사용 (프로덕션)
1. "Domains" 메뉴 클릭
2. "Add Domain" 클릭
3. 도메인 입력 (예: `yourdomain.com`)
4. DNS 레코드 추가:
   - TXT 레코드
   - MX 레코드
   - CNAME 레코드
5. "Verify DNS Records" 클릭
6. 인증 완료 후 사용 가능
   - 발신자: `noreply@yourdomain.com`

## 4. .env 파일 설정

프로젝트 루트에 `.env` 파일을 생성하고 다음 내용을 입력:

```env
# Resend API Key (2단계에서 복사한 키)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 오류 보고를 받을 이메일 주소
ERROR_REPORT_EMAIL=neohum77@gmail.com

# 발신자 이메일
# 옵션 A (테스트): onboarding@resend.dev
# 옵션 B (프로덕션): noreply@yourdomain.com
FROM_EMAIL=onboarding@resend.dev
```

## 5. 백엔드 서버 실행

```bash
cd backend
python main.py
```

서버가 http://localhost:8000 에서 실행됩니다.

## 6. 테스트

### 방법 1: 브라우저에서 테스트
1. 프론트엔드 실행 (`cd frontend && npm run dev`)
2. 백엔드 실행 (`cd backend && python main.py`)
3. 브라우저에서 EduPy 접속
4. 의도적으로 오류 발생:
   ```python
   print(undefined_variable)
   ```
5. "🐛 오류 보고" 버튼 클릭
6. `neohum77@gmail.com`에서 이메일 확인

### 방법 2: API 직접 테스트
```bash
curl -X POST http://localhost:8000/api/error-report \
  -H "Content-Type: application/json" \
  -d '{
    "level": "Level 1: 출력과 문자열",
    "activity": "1-1 - 한 줄 자기소개",
    "error_message": "NameError: name '\''x'\'' is not defined",
    "user_code": "print(x)",
    "timestamp": "2025-11-14 오후 3:45:23"
  }'
```

## 7. 이메일 형식

수신되는 이메일은 다음과 같은 형식입니다:

```
제목: [EduPy] 오류 보고 - Level 1: 출력과 문자열

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📍 발생 위치
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Level: Level 1: 출력과 문자열
Activity: 1-1 - 한 줄 자기소개

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ 오류 메시지
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
NameError: name 'x' is not defined

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💻 사용자 코드
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
print(x)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🕐 발생 시간
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2025-11-14 오후 3:45:23
```

## 8. 무료 플랜 제한

- **월 3,000개 이메일**
- **일 100개 이메일**
- 충분히 사용 가능한 양

## 9. 문제 해결

### API Key 오류
```
detail: "이메일 발송 실패: ..."
```
→ `.env` 파일의 `RESEND_API_KEY` 확인

### 발신자 이메일 오류
```
detail: "이메일 발송 실패: Invalid from address"
```
→ `.env` 파일의 `FROM_EMAIL`을 `onboarding@resend.dev`로 변경

### CORS 오류
```
Access to fetch at 'http://localhost:8000/api/error-report' has been blocked by CORS policy
```
→ 백엔드 서버가 실행 중인지 확인

## 10. 참고 링크

- Resend 공식 문서: https://resend.com/docs
- Python SDK: https://resend.com/docs/send-with-python
- API Reference: https://resend.com/docs/api-reference/introduction

